services:
  document-agent:
    environment:
      DEPLOYMENT_ENVIRONMENT: development
      OTEL_TRACE_CONTENT_ENABLED: "true"
      OTEL_TRACE_CONTENT_MAX_CHARS: ${OTEL_TRACE_CONTENT_MAX_CHARS:-12000}
      TRACELOOP_TRACE_CONTENT: "true"
